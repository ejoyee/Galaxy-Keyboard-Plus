# 애플리케이션 기본 설정
spring.application.name=auth
server.port=8091

# 로깅 설정
logging.level.com.moca.auth=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# 액추에이터 엔드포인트 설정
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# R2DBC 설정 - 라우팅 테스트만 하는 경우 아래 설정 불필요 
# 추후 데이터베이스 연결이 필요할 때 주석 해제

# 로컬 테스트용 설정
# spring.r2dbc.url=r2dbc:postgresql://localhost:5432/postgres
# spring.r2dbc.username=postgres
# spring.r2dbc.password=postgres

# Docker 컨테이너 환경에서는 다음 설정 사용
# spring.r2dbc.url=r2dbc:postgresql://db:5432/${POSTGRE_DB_NAME}
# spring.r2dbc.username=${POSTGRE_USERNAME}
# spring.r2dbc.password=${POSTGRE_PASSWORD}

# R2DBC 연결 풀 설정
# spring.r2dbc.pool.initial-size=5
# spring.r2dbc.pool.max-size=20
# spring.r2dbc.pool.max-idle-time=30m
